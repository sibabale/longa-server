{
  "name": "longa-server-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Scripts for longa-server (e.g. seed)",
  "scripts": {
    "seed": "node scripts/seed-trips.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
